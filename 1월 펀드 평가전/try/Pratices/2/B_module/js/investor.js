export default class Investor {
    constructor(list) {
        this.list = list;
        console.log("!");
        console.log(list);

        // this.app = app;

        this.loading();
    }

    loading() {
    }
}